# Composer — vendor/ is tracked in this repo (clone without `composer install` if desired).
# To ignore vendor again, uncomment: /vendor/

# Environment & secrets
.env
.env.*
!.env.example

# Logs & cache
*.log
*.cache
.phpunit.result.cache

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.idea/
*.swp
*.swo
*~

# Optional local tooling
/node_modules/
